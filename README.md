markdown_content = """# Suma de Dos Números en Python

Este es un programa sencillo y educativo escrito en Python que solicita al usuario ingresar dos números por la consola, realiza la operación de suma y muestra el resultado final en la pantalla.

## 🚀 Características
- **Entrada dinámica:** Permite al usuario interactuar con el programa ingresando cualquier número.
- **Soporte decimal:** Utiliza el tipo de dato `float`, lo que permite sumar tanto números enteros (ej. 5, 10) como decimales (ej. 3.5, 4.2).
- **Código didáctico:** Ideal para personas que están iniciando en el mundo de la programación con Python.

## 📋 Requisitos
Para ejecutar este programa, necesitas tener instalado Python en tu computadora (versión 3.0 o superior recomendada). Puedes descargarlo desde [python.org](https://www.python.org/).

## ⚙️ Cómo ejecutar el programa

1. **Guardar el código:** Copia el código fuente y guárdalo en un archivo llamado `suma.py`.
2. **Abrir la terminal:** Abre la terminal, consola o símbolo del sistema (CMD) y navega hasta la carpeta donde guardaste el archivo.
3. **Ejecutar el comando:** Ejecuta el siguiente comando:
```bash
   python suma.py